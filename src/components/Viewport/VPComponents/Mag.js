/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
*/

import React, { useRef } from "react";
import { useGLTF } from "@react-three/drei";

export default function Model({ ...props }) {
  const group = useRef();
  const { nodes, materials } = useGLTF("/Mag.glb");
  return (
    <group ref={group} {...props} dispose={null}>
      <mesh geometry={nodes.MagSuit.geometry}>
        <meshStandardMaterial
          color={color}
          material={materials.Ghost}
          // aoMap={materials.mesh.aoMap}
          // normalMap={materials.mesh.normalMap}
          // roughnessMap={materials.mesh.roughnessMap}
          // metalnessMap={materials.mesh.metalnessMap}
          envMapIntensity={0.8}
        />
      </mesh>
    </group>
  );
}

// useGLTF.preload("/Mag.glb");
